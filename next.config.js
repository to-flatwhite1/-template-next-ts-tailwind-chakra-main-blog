/** @type {import('next').NextConfig} */
const nextConfig = {
    /* config options here */
    images: {
        domains: ['img.youtube.com', 'images.unsplash.com'], // YouTube 썸네일 이미지 도메인 추가
    },
};

module.exports = nextConfig;
